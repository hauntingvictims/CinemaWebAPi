namespace CinemaWebApi.Models
{ 
    public enum HallType { Regular, Vip }
    public enum ShowingStatus { Active, Canceled }

    public enum TicketStatus { Available, Purchased, Reserved }
    public enum UserType { Admin, User }
}